'''Write a code that takes a list of words as input and returns a dictionary where the keys
are unique words and the values are the frequencies of those words in the input list'''
from itertools import count

list1=list(input("List: ").split())

dictionary={i: list1.count(i) for i in list1}

print("Dictionary: ",dictionary)
